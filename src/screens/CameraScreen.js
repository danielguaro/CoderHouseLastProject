import {
	KeyboardAvoidingView,
	ScrollView,
	StyleSheet,
	Text,
	TextInput,
	TouchableOpacity,
	View,
} from 'react-native';

import React from 'react';

export const CameraScreen = ({ navigation }) => {
	return (
		<View>
			<Text>Aqui va la camera Screen</Text>
		</View>
	);
};
